models for fact checking backend

## Running locally ##
* (probably setup a virtualenv)
* `pip install -r requirements.txt`
* `python fact-play.py`
This will start up a local debug server of port 5000